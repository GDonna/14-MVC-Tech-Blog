
{{!-- TO DO add a form to edit post

    -add text area with post.body
    -need onSubmit event listener

    connect edit-post.handlebars to edit-post.js
 --}}

 <form action="/post/{{post.id}}/edit" method="POST">
   <label class="form-label" for="post-title">Title</label>
   <input type="text" name="title" value="{{post.title}}" class="form-input" />

   <label class="form-label" for="post-body">Body</label>
   <textarea name="body" class="form-input">{{post.body}}</textarea>

   <button type="submit">Save Changes</button>
</form>